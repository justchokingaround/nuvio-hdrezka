{
  "name": "nuvio-hdrezka",
  "version": "0.1.0",
  "description": "Nuvio provider for HDRezka — scrapes stream URLs from hdrezka-home.tv using TMDB ID/title.",
  "type": "module",
  "scripts": {
    "build": "node build.cjs",
    "build:watch": "node build.cjs --watch",
    "test": "node test.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "cheerio-without-node-native": "^0.20.2",
    "esbuild": "^0.21.0"
  },
  "engines": {
    "node": ">=18"
  },
  "private": true
}
